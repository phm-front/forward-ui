import { withInstall } from '@forward-ui/utils';
import scrollbar from './src/scrollbar.vue';

export const FScrollbar = withInstall(scrollbar)
export default FScrollbar

export * from './src/scrollbar'
